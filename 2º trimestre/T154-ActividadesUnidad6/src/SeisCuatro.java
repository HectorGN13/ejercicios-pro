import java.util.LinkedList;
import java.util.Stack;

public class SeisCuatro {
    /**
     * Actividad 6.4 Buscar en la API de Java si la clase LinkedList tiene los métodos de la clase Stack
     */
    // empty() no lo tiene
    // peek()  si, lo implenta de la interface Queue
    // pop()   si, pero lo implemta de la interface Deque
    // push()  si, pero lo implemta de la interface Deque
    // search() no

    Stack<Integer> numeros = new Stack<>();
    LinkedList<Integer> num = new LinkedList<>();

}
